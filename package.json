{
  "name": "xclusiv-freight",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Offline-first logistics & document management console for Exclusiv Freight",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vite build && node tests/smoke.test.js",
    "prebuild": "node tools/generate-og-image.js"
  },
  "devDependencies": {
    "jsdom": "^26.0.0",
    "sharp": "^0.35.3",
    "vite": "^7.0.0"
  }
}
